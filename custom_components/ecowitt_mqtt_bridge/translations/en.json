{
  "title": "Ecowitt MQTT Bridge",
  "config": {
    "step": {
      "user": {
        "title": "Setup Ecowitt MQTT Bridge",
        "description": "Subscribe to Ecowitt MQTT uploads and publish HA Discovery sensors.",
        "data": {
          "in_topic": "MQTT input topic (e.g. ecowitt/# or ecowitt/048308... )",
          "discovery_prefix": "Discovery prefix (default: homeassistant)",
          "state_prefix": "State prefix (default: ecowitt_ha)",
          "use_local_api": "Use local API for device metadata (fallback)",
          "base_url": "Gateway base URL (e.g. http://192.168.0.46)",
          "lan_timeout": "LAN timeout (s)",
          "map_refresh_sec": "Sensor map refresh (s)",
          "publish_lan_common": "Publish LAN common_list (may duplicate MQTT data)",
          "unit_temperature": "Temperature unit override (C / F, blank = HA)",
          "unit_wind": "Wind unit override (m/s / km/h / mph, blank = HA)",
          "unit_rain": "Rain unit override (mm / in, blank = HA)",
          "unit_pressure": "Pressure unit override (hPa / inHg, blank = HA)",
          "custom_sensors": "Custom sensors JSON (list of objects)"
        }
      }
    }
  }
}
